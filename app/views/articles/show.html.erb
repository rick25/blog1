<p>
	<strong>Titulo:</strong>
	<%= @article.title %>
</p>
<p>
	<strong>Texto:</strong>
	<%= @article.text %>
</p>

<h2>Commentarios</h2>
<%= render @article.comments %>

<h2>Agregar un comentario:</h2>
<%= render 'comments/form' %>
<%= link_to 'Volver', articles_path %> | <%= link_to 'Editar', edit_article_path(@article) %>