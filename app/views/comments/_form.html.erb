<%= form_for([@article, @article.comments.build]) do |f| %>
  	<p>
    	<%= f.label :comentador %><br>
    	<%= f.text_field :commenter %>
  	</p>
  	<p>
    	<%= f.label :cuerpo %><br>
    	<%= f.text_area :body %>
  	</p>
  	<p>
    	<%= f.submit 'Crear Comentario' %>
  	</p>
<% end %>